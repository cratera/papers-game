{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "pretty": true,
    "baseUrl": ".",
    // These aliases must be duplicated in babel.config.js
    "paths": {
      "@src/*": ["./src/*"]
    }
  }
}
